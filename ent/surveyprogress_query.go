// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"roofix/ent/apiuser"
	"roofix/ent/predicate"
	"roofix/ent/survey"
	"roofix/ent/surveyprogress"
	"roofix/ent/user"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// SurveyProgressQuery is the builder for querying SurveyProgress entities.
type SurveyProgressQuery struct {
	config
	ctx            *QueryContext
	order          []surveyprogress.OrderOption
	inters         []Interceptor
	predicates     []predicate.SurveyProgress
	withSurvey     *SurveyQuery
	withCreator    *UserQuery
	withCreatorAPI *ApiUserQuery
	withFKs        bool
	loadTotal      []func(context.Context, []*SurveyProgress) error
	modifiers      []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the SurveyProgressQuery builder.
func (spq *SurveyProgressQuery) Where(ps ...predicate.SurveyProgress) *SurveyProgressQuery {
	spq.predicates = append(spq.predicates, ps...)
	return spq
}

// Limit the number of records to be returned by this query.
func (spq *SurveyProgressQuery) Limit(limit int) *SurveyProgressQuery {
	spq.ctx.Limit = &limit
	return spq
}

// Offset to start from.
func (spq *SurveyProgressQuery) Offset(offset int) *SurveyProgressQuery {
	spq.ctx.Offset = &offset
	return spq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (spq *SurveyProgressQuery) Unique(unique bool) *SurveyProgressQuery {
	spq.ctx.Unique = &unique
	return spq
}

// Order specifies how the records should be ordered.
func (spq *SurveyProgressQuery) Order(o ...surveyprogress.OrderOption) *SurveyProgressQuery {
	spq.order = append(spq.order, o...)
	return spq
}

// QuerySurvey chains the current query on the "survey" edge.
func (spq *SurveyProgressQuery) QuerySurvey() *SurveyQuery {
	query := (&SurveyClient{config: spq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := spq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := spq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(surveyprogress.Table, surveyprogress.FieldID, selector),
			sqlgraph.To(survey.Table, survey.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, surveyprogress.SurveyTable, surveyprogress.SurveyColumn),
		)
		fromU = sqlgraph.SetNeighbors(spq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryCreator chains the current query on the "creator" edge.
func (spq *SurveyProgressQuery) QueryCreator() *UserQuery {
	query := (&UserClient{config: spq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := spq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := spq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(surveyprogress.Table, surveyprogress.FieldID, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, surveyprogress.CreatorTable, surveyprogress.CreatorColumn),
		)
		fromU = sqlgraph.SetNeighbors(spq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryCreatorAPI chains the current query on the "creator_api" edge.
func (spq *SurveyProgressQuery) QueryCreatorAPI() *ApiUserQuery {
	query := (&ApiUserClient{config: spq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := spq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := spq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(surveyprogress.Table, surveyprogress.FieldID, selector),
			sqlgraph.To(apiuser.Table, apiuser.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, surveyprogress.CreatorAPITable, surveyprogress.CreatorAPIColumn),
		)
		fromU = sqlgraph.SetNeighbors(spq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first SurveyProgress entity from the query.
// Returns a *NotFoundError when no SurveyProgress was found.
func (spq *SurveyProgressQuery) First(ctx context.Context) (*SurveyProgress, error) {
	nodes, err := spq.Limit(1).All(setContextOp(ctx, spq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{surveyprogress.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (spq *SurveyProgressQuery) FirstX(ctx context.Context) *SurveyProgress {
	node, err := spq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first SurveyProgress ID from the query.
// Returns a *NotFoundError when no SurveyProgress ID was found.
func (spq *SurveyProgressQuery) FirstID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = spq.Limit(1).IDs(setContextOp(ctx, spq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{surveyprogress.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (spq *SurveyProgressQuery) FirstIDX(ctx context.Context) string {
	id, err := spq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single SurveyProgress entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one SurveyProgress entity is found.
// Returns a *NotFoundError when no SurveyProgress entities are found.
func (spq *SurveyProgressQuery) Only(ctx context.Context) (*SurveyProgress, error) {
	nodes, err := spq.Limit(2).All(setContextOp(ctx, spq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{surveyprogress.Label}
	default:
		return nil, &NotSingularError{surveyprogress.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (spq *SurveyProgressQuery) OnlyX(ctx context.Context) *SurveyProgress {
	node, err := spq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only SurveyProgress ID in the query.
// Returns a *NotSingularError when more than one SurveyProgress ID is found.
// Returns a *NotFoundError when no entities are found.
func (spq *SurveyProgressQuery) OnlyID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = spq.Limit(2).IDs(setContextOp(ctx, spq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{surveyprogress.Label}
	default:
		err = &NotSingularError{surveyprogress.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (spq *SurveyProgressQuery) OnlyIDX(ctx context.Context) string {
	id, err := spq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of SurveyProgresses.
func (spq *SurveyProgressQuery) All(ctx context.Context) ([]*SurveyProgress, error) {
	ctx = setContextOp(ctx, spq.ctx, "All")
	if err := spq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*SurveyProgress, *SurveyProgressQuery]()
	return withInterceptors[[]*SurveyProgress](ctx, spq, qr, spq.inters)
}

// AllX is like All, but panics if an error occurs.
func (spq *SurveyProgressQuery) AllX(ctx context.Context) []*SurveyProgress {
	nodes, err := spq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of SurveyProgress IDs.
func (spq *SurveyProgressQuery) IDs(ctx context.Context) (ids []string, err error) {
	if spq.ctx.Unique == nil && spq.path != nil {
		spq.Unique(true)
	}
	ctx = setContextOp(ctx, spq.ctx, "IDs")
	if err = spq.Select(surveyprogress.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (spq *SurveyProgressQuery) IDsX(ctx context.Context) []string {
	ids, err := spq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (spq *SurveyProgressQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, spq.ctx, "Count")
	if err := spq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, spq, querierCount[*SurveyProgressQuery](), spq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (spq *SurveyProgressQuery) CountX(ctx context.Context) int {
	count, err := spq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (spq *SurveyProgressQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, spq.ctx, "Exist")
	switch _, err := spq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (spq *SurveyProgressQuery) ExistX(ctx context.Context) bool {
	exist, err := spq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the SurveyProgressQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (spq *SurveyProgressQuery) Clone() *SurveyProgressQuery {
	if spq == nil {
		return nil
	}
	return &SurveyProgressQuery{
		config:         spq.config,
		ctx:            spq.ctx.Clone(),
		order:          append([]surveyprogress.OrderOption{}, spq.order...),
		inters:         append([]Interceptor{}, spq.inters...),
		predicates:     append([]predicate.SurveyProgress{}, spq.predicates...),
		withSurvey:     spq.withSurvey.Clone(),
		withCreator:    spq.withCreator.Clone(),
		withCreatorAPI: spq.withCreatorAPI.Clone(),
		// clone intermediate query.
		sql:  spq.sql.Clone(),
		path: spq.path,
	}
}

// WithSurvey tells the query-builder to eager-load the nodes that are connected to
// the "survey" edge. The optional arguments are used to configure the query builder of the edge.
func (spq *SurveyProgressQuery) WithSurvey(opts ...func(*SurveyQuery)) *SurveyProgressQuery {
	query := (&SurveyClient{config: spq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	spq.withSurvey = query
	return spq
}

// WithCreator tells the query-builder to eager-load the nodes that are connected to
// the "creator" edge. The optional arguments are used to configure the query builder of the edge.
func (spq *SurveyProgressQuery) WithCreator(opts ...func(*UserQuery)) *SurveyProgressQuery {
	query := (&UserClient{config: spq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	spq.withCreator = query
	return spq
}

// WithCreatorAPI tells the query-builder to eager-load the nodes that are connected to
// the "creator_api" edge. The optional arguments are used to configure the query builder of the edge.
func (spq *SurveyProgressQuery) WithCreatorAPI(opts ...func(*ApiUserQuery)) *SurveyProgressQuery {
	query := (&ApiUserClient{config: spq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	spq.withCreatorAPI = query
	return spq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"createdAt"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.SurveyProgress.Query().
//		GroupBy(surveyprogress.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (spq *SurveyProgressQuery) GroupBy(field string, fields ...string) *SurveyProgressGroupBy {
	spq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &SurveyProgressGroupBy{build: spq}
	grbuild.flds = &spq.ctx.Fields
	grbuild.label = surveyprogress.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"createdAt"`
//	}
//
//	client.SurveyProgress.Query().
//		Select(surveyprogress.FieldCreatedAt).
//		Scan(ctx, &v)
func (spq *SurveyProgressQuery) Select(fields ...string) *SurveyProgressSelect {
	spq.ctx.Fields = append(spq.ctx.Fields, fields...)
	sbuild := &SurveyProgressSelect{SurveyProgressQuery: spq}
	sbuild.label = surveyprogress.Label
	sbuild.flds, sbuild.scan = &spq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a SurveyProgressSelect configured with the given aggregations.
func (spq *SurveyProgressQuery) Aggregate(fns ...AggregateFunc) *SurveyProgressSelect {
	return spq.Select().Aggregate(fns...)
}

func (spq *SurveyProgressQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range spq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, spq); err != nil {
				return err
			}
		}
	}
	for _, f := range spq.ctx.Fields {
		if !surveyprogress.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if spq.path != nil {
		prev, err := spq.path(ctx)
		if err != nil {
			return err
		}
		spq.sql = prev
	}
	return nil
}

func (spq *SurveyProgressQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*SurveyProgress, error) {
	var (
		nodes       = []*SurveyProgress{}
		withFKs     = spq.withFKs
		_spec       = spq.querySpec()
		loadedTypes = [3]bool{
			spq.withSurvey != nil,
			spq.withCreator != nil,
			spq.withCreatorAPI != nil,
		}
	)
	if spq.withSurvey != nil || spq.withCreator != nil || spq.withCreatorAPI != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, surveyprogress.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*SurveyProgress).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &SurveyProgress{config: spq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(spq.modifiers) > 0 {
		_spec.Modifiers = spq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, spq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := spq.withSurvey; query != nil {
		if err := spq.loadSurvey(ctx, query, nodes, nil,
			func(n *SurveyProgress, e *Survey) { n.Edges.Survey = e }); err != nil {
			return nil, err
		}
	}
	if query := spq.withCreator; query != nil {
		if err := spq.loadCreator(ctx, query, nodes, nil,
			func(n *SurveyProgress, e *User) { n.Edges.Creator = e }); err != nil {
			return nil, err
		}
	}
	if query := spq.withCreatorAPI; query != nil {
		if err := spq.loadCreatorAPI(ctx, query, nodes, nil,
			func(n *SurveyProgress, e *ApiUser) { n.Edges.CreatorAPI = e }); err != nil {
			return nil, err
		}
	}
	for i := range spq.loadTotal {
		if err := spq.loadTotal[i](ctx, nodes); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (spq *SurveyProgressQuery) loadSurvey(ctx context.Context, query *SurveyQuery, nodes []*SurveyProgress, init func(*SurveyProgress), assign func(*SurveyProgress, *Survey)) error {
	ids := make([]string, 0, len(nodes))
	nodeids := make(map[string][]*SurveyProgress)
	for i := range nodes {
		if nodes[i].survey_id == nil {
			continue
		}
		fk := *nodes[i].survey_id
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(survey.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "survey_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (spq *SurveyProgressQuery) loadCreator(ctx context.Context, query *UserQuery, nodes []*SurveyProgress, init func(*SurveyProgress), assign func(*SurveyProgress, *User)) error {
	ids := make([]string, 0, len(nodes))
	nodeids := make(map[string][]*SurveyProgress)
	for i := range nodes {
		if nodes[i].creator == nil {
			continue
		}
		fk := *nodes[i].creator
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(user.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "creator" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (spq *SurveyProgressQuery) loadCreatorAPI(ctx context.Context, query *ApiUserQuery, nodes []*SurveyProgress, init func(*SurveyProgress), assign func(*SurveyProgress, *ApiUser)) error {
	ids := make([]string, 0, len(nodes))
	nodeids := make(map[string][]*SurveyProgress)
	for i := range nodes {
		if nodes[i].api_user_id == nil {
			continue
		}
		fk := *nodes[i].api_user_id
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(apiuser.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "api_user_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (spq *SurveyProgressQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := spq.querySpec()
	if len(spq.modifiers) > 0 {
		_spec.Modifiers = spq.modifiers
	}
	_spec.Node.Columns = spq.ctx.Fields
	if len(spq.ctx.Fields) > 0 {
		_spec.Unique = spq.ctx.Unique != nil && *spq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, spq.driver, _spec)
}

func (spq *SurveyProgressQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(surveyprogress.Table, surveyprogress.Columns, sqlgraph.NewFieldSpec(surveyprogress.FieldID, field.TypeString))
	_spec.From = spq.sql
	if unique := spq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if spq.path != nil {
		_spec.Unique = true
	}
	if fields := spq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, surveyprogress.FieldID)
		for i := range fields {
			if fields[i] != surveyprogress.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := spq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := spq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := spq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := spq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (spq *SurveyProgressQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(spq.driver.Dialect())
	t1 := builder.Table(surveyprogress.Table)
	columns := spq.ctx.Fields
	if len(columns) == 0 {
		columns = surveyprogress.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if spq.sql != nil {
		selector = spq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if spq.ctx.Unique != nil && *spq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range spq.modifiers {
		m(selector)
	}
	for _, p := range spq.predicates {
		p(selector)
	}
	for _, p := range spq.order {
		p(selector)
	}
	if offset := spq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := spq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// Modify adds a query modifier for attaching custom logic to queries.
func (spq *SurveyProgressQuery) Modify(modifiers ...func(s *sql.Selector)) *SurveyProgressSelect {
	spq.modifiers = append(spq.modifiers, modifiers...)
	return spq.Select()
}

// SurveyProgressGroupBy is the group-by builder for SurveyProgress entities.
type SurveyProgressGroupBy struct {
	selector
	build *SurveyProgressQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (spgb *SurveyProgressGroupBy) Aggregate(fns ...AggregateFunc) *SurveyProgressGroupBy {
	spgb.fns = append(spgb.fns, fns...)
	return spgb
}

// Scan applies the selector query and scans the result into the given value.
func (spgb *SurveyProgressGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, spgb.build.ctx, "GroupBy")
	if err := spgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SurveyProgressQuery, *SurveyProgressGroupBy](ctx, spgb.build, spgb, spgb.build.inters, v)
}

func (spgb *SurveyProgressGroupBy) sqlScan(ctx context.Context, root *SurveyProgressQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(spgb.fns))
	for _, fn := range spgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*spgb.flds)+len(spgb.fns))
		for _, f := range *spgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*spgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := spgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// SurveyProgressSelect is the builder for selecting fields of SurveyProgress entities.
type SurveyProgressSelect struct {
	*SurveyProgressQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (sps *SurveyProgressSelect) Aggregate(fns ...AggregateFunc) *SurveyProgressSelect {
	sps.fns = append(sps.fns, fns...)
	return sps
}

// Scan applies the selector query and scans the result into the given value.
func (sps *SurveyProgressSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, sps.ctx, "Select")
	if err := sps.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SurveyProgressQuery, *SurveyProgressSelect](ctx, sps.SurveyProgressQuery, sps, sps.inters, v)
}

func (sps *SurveyProgressSelect) sqlScan(ctx context.Context, root *SurveyProgressQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(sps.fns))
	for _, fn := range sps.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*sps.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := sps.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (sps *SurveyProgressSelect) Modify(modifiers ...func(s *sql.Selector)) *SurveyProgressSelect {
	sps.modifiers = append(sps.modifiers, modifiers...)
	return sps
}
