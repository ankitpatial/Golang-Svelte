// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"roofix/ent/notifysetting"
	"roofix/ent/user"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// NotifySetting is the model entity for the NotifySetting schema.
type NotifySetting struct {
	config `json:"-"`
	// ID of the ent.
	ID string `json:"id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"createdAt"`
	// TopicID holds the value of the "topic_id" field.
	TopicID string `json:"topic_id,omitempty"`
	// ReceiveEmail holds the value of the "receive_email" field.
	ReceiveEmail bool `json:"receive_email,omitempty"`
	// ReceiveSms holds the value of the "receive_sms" field.
	ReceiveSms bool `json:"receive_sms,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the NotifySettingQuery when eager-loading is set.
	Edges        NotifySettingEdges `json:"edges"`
	user_id      *string
	selectValues sql.SelectValues
}

// NotifySettingEdges holds the relations/edges for other nodes in the graph.
type NotifySettingEdges struct {
	// User holds the value of the user edge.
	User *User `json:"user,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
	// totalCount holds the count of the edges above.
	totalCount [1]map[string]int
}

// UserOrErr returns the User value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e NotifySettingEdges) UserOrErr() (*User, error) {
	if e.loadedTypes[0] {
		if e.User == nil {
			// Edge was loaded but was not found.
			return nil, &NotFoundError{label: user.Label}
		}
		return e.User, nil
	}
	return nil, &NotLoadedError{edge: "user"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*NotifySetting) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case notifysetting.FieldReceiveEmail, notifysetting.FieldReceiveSms:
			values[i] = new(sql.NullBool)
		case notifysetting.FieldID, notifysetting.FieldTopicID:
			values[i] = new(sql.NullString)
		case notifysetting.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		case notifysetting.ForeignKeys[0]: // user_id
			values[i] = new(sql.NullString)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the NotifySetting fields.
func (ns *NotifySetting) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case notifysetting.FieldID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value.Valid {
				ns.ID = value.String
			}
		case notifysetting.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				ns.CreatedAt = value.Time
			}
		case notifysetting.FieldTopicID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field topic_id", values[i])
			} else if value.Valid {
				ns.TopicID = value.String
			}
		case notifysetting.FieldReceiveEmail:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field receive_email", values[i])
			} else if value.Valid {
				ns.ReceiveEmail = value.Bool
			}
		case notifysetting.FieldReceiveSms:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field receive_sms", values[i])
			} else if value.Valid {
				ns.ReceiveSms = value.Bool
			}
		case notifysetting.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field user_id", values[i])
			} else if value.Valid {
				ns.user_id = new(string)
				*ns.user_id = value.String
			}
		default:
			ns.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the NotifySetting.
// This includes values selected through modifiers, order, etc.
func (ns *NotifySetting) Value(name string) (ent.Value, error) {
	return ns.selectValues.Get(name)
}

// QueryUser queries the "user" edge of the NotifySetting entity.
func (ns *NotifySetting) QueryUser() *UserQuery {
	return NewNotifySettingClient(ns.config).QueryUser(ns)
}

// Update returns a builder for updating this NotifySetting.
// Note that you need to call NotifySetting.Unwrap() before calling this method if this NotifySetting
// was returned from a transaction, and the transaction was committed or rolled back.
func (ns *NotifySetting) Update() *NotifySettingUpdateOne {
	return NewNotifySettingClient(ns.config).UpdateOne(ns)
}

// Unwrap unwraps the NotifySetting entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (ns *NotifySetting) Unwrap() *NotifySetting {
	_tx, ok := ns.config.driver.(*txDriver)
	if !ok {
		panic("ent: NotifySetting is not a transactional entity")
	}
	ns.config.driver = _tx.drv
	return ns
}

// String implements the fmt.Stringer.
func (ns *NotifySetting) String() string {
	var builder strings.Builder
	builder.WriteString("NotifySetting(")
	builder.WriteString(fmt.Sprintf("id=%v, ", ns.ID))
	builder.WriteString("created_at=")
	builder.WriteString(ns.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("topic_id=")
	builder.WriteString(ns.TopicID)
	builder.WriteString(", ")
	builder.WriteString("receive_email=")
	builder.WriteString(fmt.Sprintf("%v", ns.ReceiveEmail))
	builder.WriteString(", ")
	builder.WriteString("receive_sms=")
	builder.WriteString(fmt.Sprintf("%v", ns.ReceiveSms))
	builder.WriteByte(')')
	return builder.String()
}

// NotifySettings is a parsable slice of NotifySetting.
type NotifySettings []*NotifySetting
