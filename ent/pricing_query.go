// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"roofix/ent/postalcode"
	"roofix/ent/predicate"
	"roofix/ent/pricing"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// PricingQuery is the builder for querying Pricing entities.
type PricingQuery struct {
	config
	ctx        *QueryContext
	order      []pricing.OrderOption
	inters     []Interceptor
	predicates []predicate.Pricing
	withPostal *PostalCodeQuery
	withFKs    bool
	loadTotal  []func(context.Context, []*Pricing) error
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the PricingQuery builder.
func (pq *PricingQuery) Where(ps ...predicate.Pricing) *PricingQuery {
	pq.predicates = append(pq.predicates, ps...)
	return pq
}

// Limit the number of records to be returned by this query.
func (pq *PricingQuery) Limit(limit int) *PricingQuery {
	pq.ctx.Limit = &limit
	return pq
}

// Offset to start from.
func (pq *PricingQuery) Offset(offset int) *PricingQuery {
	pq.ctx.Offset = &offset
	return pq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (pq *PricingQuery) Unique(unique bool) *PricingQuery {
	pq.ctx.Unique = &unique
	return pq
}

// Order specifies how the records should be ordered.
func (pq *PricingQuery) Order(o ...pricing.OrderOption) *PricingQuery {
	pq.order = append(pq.order, o...)
	return pq
}

// QueryPostal chains the current query on the "postal" edge.
func (pq *PricingQuery) QueryPostal() *PostalCodeQuery {
	query := (&PostalCodeClient{config: pq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := pq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := pq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(pricing.Table, pricing.FieldID, selector),
			sqlgraph.To(postalcode.Table, postalcode.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, pricing.PostalTable, pricing.PostalColumn),
		)
		fromU = sqlgraph.SetNeighbors(pq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first Pricing entity from the query.
// Returns a *NotFoundError when no Pricing was found.
func (pq *PricingQuery) First(ctx context.Context) (*Pricing, error) {
	nodes, err := pq.Limit(1).All(setContextOp(ctx, pq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{pricing.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (pq *PricingQuery) FirstX(ctx context.Context) *Pricing {
	node, err := pq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first Pricing ID from the query.
// Returns a *NotFoundError when no Pricing ID was found.
func (pq *PricingQuery) FirstID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = pq.Limit(1).IDs(setContextOp(ctx, pq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{pricing.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (pq *PricingQuery) FirstIDX(ctx context.Context) string {
	id, err := pq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single Pricing entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one Pricing entity is found.
// Returns a *NotFoundError when no Pricing entities are found.
func (pq *PricingQuery) Only(ctx context.Context) (*Pricing, error) {
	nodes, err := pq.Limit(2).All(setContextOp(ctx, pq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{pricing.Label}
	default:
		return nil, &NotSingularError{pricing.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (pq *PricingQuery) OnlyX(ctx context.Context) *Pricing {
	node, err := pq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only Pricing ID in the query.
// Returns a *NotSingularError when more than one Pricing ID is found.
// Returns a *NotFoundError when no entities are found.
func (pq *PricingQuery) OnlyID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = pq.Limit(2).IDs(setContextOp(ctx, pq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{pricing.Label}
	default:
		err = &NotSingularError{pricing.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (pq *PricingQuery) OnlyIDX(ctx context.Context) string {
	id, err := pq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of Pricings.
func (pq *PricingQuery) All(ctx context.Context) ([]*Pricing, error) {
	ctx = setContextOp(ctx, pq.ctx, "All")
	if err := pq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*Pricing, *PricingQuery]()
	return withInterceptors[[]*Pricing](ctx, pq, qr, pq.inters)
}

// AllX is like All, but panics if an error occurs.
func (pq *PricingQuery) AllX(ctx context.Context) []*Pricing {
	nodes, err := pq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of Pricing IDs.
func (pq *PricingQuery) IDs(ctx context.Context) (ids []string, err error) {
	if pq.ctx.Unique == nil && pq.path != nil {
		pq.Unique(true)
	}
	ctx = setContextOp(ctx, pq.ctx, "IDs")
	if err = pq.Select(pricing.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (pq *PricingQuery) IDsX(ctx context.Context) []string {
	ids, err := pq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (pq *PricingQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, pq.ctx, "Count")
	if err := pq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, pq, querierCount[*PricingQuery](), pq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (pq *PricingQuery) CountX(ctx context.Context) int {
	count, err := pq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (pq *PricingQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, pq.ctx, "Exist")
	switch _, err := pq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (pq *PricingQuery) ExistX(ctx context.Context) bool {
	exist, err := pq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the PricingQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (pq *PricingQuery) Clone() *PricingQuery {
	if pq == nil {
		return nil
	}
	return &PricingQuery{
		config:     pq.config,
		ctx:        pq.ctx.Clone(),
		order:      append([]pricing.OrderOption{}, pq.order...),
		inters:     append([]Interceptor{}, pq.inters...),
		predicates: append([]predicate.Pricing{}, pq.predicates...),
		withPostal: pq.withPostal.Clone(),
		// clone intermediate query.
		sql:  pq.sql.Clone(),
		path: pq.path,
	}
}

// WithPostal tells the query-builder to eager-load the nodes that are connected to
// the "postal" edge. The optional arguments are used to configure the query builder of the edge.
func (pq *PricingQuery) WithPostal(opts ...func(*PostalCodeQuery)) *PricingQuery {
	query := (&PostalCodeClient{config: pq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	pq.withPostal = query
	return pq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"createdAt"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.Pricing.Query().
//		GroupBy(pricing.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (pq *PricingQuery) GroupBy(field string, fields ...string) *PricingGroupBy {
	pq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &PricingGroupBy{build: pq}
	grbuild.flds = &pq.ctx.Fields
	grbuild.label = pricing.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"createdAt"`
//	}
//
//	client.Pricing.Query().
//		Select(pricing.FieldCreatedAt).
//		Scan(ctx, &v)
func (pq *PricingQuery) Select(fields ...string) *PricingSelect {
	pq.ctx.Fields = append(pq.ctx.Fields, fields...)
	sbuild := &PricingSelect{PricingQuery: pq}
	sbuild.label = pricing.Label
	sbuild.flds, sbuild.scan = &pq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a PricingSelect configured with the given aggregations.
func (pq *PricingQuery) Aggregate(fns ...AggregateFunc) *PricingSelect {
	return pq.Select().Aggregate(fns...)
}

func (pq *PricingQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range pq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, pq); err != nil {
				return err
			}
		}
	}
	for _, f := range pq.ctx.Fields {
		if !pricing.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if pq.path != nil {
		prev, err := pq.path(ctx)
		if err != nil {
			return err
		}
		pq.sql = prev
	}
	return nil
}

func (pq *PricingQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*Pricing, error) {
	var (
		nodes       = []*Pricing{}
		withFKs     = pq.withFKs
		_spec       = pq.querySpec()
		loadedTypes = [1]bool{
			pq.withPostal != nil,
		}
	)
	if pq.withPostal != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, pricing.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*Pricing).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &Pricing{config: pq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(pq.modifiers) > 0 {
		_spec.Modifiers = pq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, pq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := pq.withPostal; query != nil {
		if err := pq.loadPostal(ctx, query, nodes, nil,
			func(n *Pricing, e *PostalCode) { n.Edges.Postal = e }); err != nil {
			return nil, err
		}
	}
	for i := range pq.loadTotal {
		if err := pq.loadTotal[i](ctx, nodes); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (pq *PricingQuery) loadPostal(ctx context.Context, query *PostalCodeQuery, nodes []*Pricing, init func(*Pricing), assign func(*Pricing, *PostalCode)) error {
	ids := make([]string, 0, len(nodes))
	nodeids := make(map[string][]*Pricing)
	for i := range nodes {
		if nodes[i].postal_id == nil {
			continue
		}
		fk := *nodes[i].postal_id
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(postalcode.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "postal_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (pq *PricingQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := pq.querySpec()
	if len(pq.modifiers) > 0 {
		_spec.Modifiers = pq.modifiers
	}
	_spec.Node.Columns = pq.ctx.Fields
	if len(pq.ctx.Fields) > 0 {
		_spec.Unique = pq.ctx.Unique != nil && *pq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, pq.driver, _spec)
}

func (pq *PricingQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(pricing.Table, pricing.Columns, sqlgraph.NewFieldSpec(pricing.FieldID, field.TypeString))
	_spec.From = pq.sql
	if unique := pq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if pq.path != nil {
		_spec.Unique = true
	}
	if fields := pq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, pricing.FieldID)
		for i := range fields {
			if fields[i] != pricing.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := pq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := pq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := pq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := pq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (pq *PricingQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(pq.driver.Dialect())
	t1 := builder.Table(pricing.Table)
	columns := pq.ctx.Fields
	if len(columns) == 0 {
		columns = pricing.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if pq.sql != nil {
		selector = pq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if pq.ctx.Unique != nil && *pq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range pq.modifiers {
		m(selector)
	}
	for _, p := range pq.predicates {
		p(selector)
	}
	for _, p := range pq.order {
		p(selector)
	}
	if offset := pq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := pq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// Modify adds a query modifier for attaching custom logic to queries.
func (pq *PricingQuery) Modify(modifiers ...func(s *sql.Selector)) *PricingSelect {
	pq.modifiers = append(pq.modifiers, modifiers...)
	return pq.Select()
}

// PricingGroupBy is the group-by builder for Pricing entities.
type PricingGroupBy struct {
	selector
	build *PricingQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (pgb *PricingGroupBy) Aggregate(fns ...AggregateFunc) *PricingGroupBy {
	pgb.fns = append(pgb.fns, fns...)
	return pgb
}

// Scan applies the selector query and scans the result into the given value.
func (pgb *PricingGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, pgb.build.ctx, "GroupBy")
	if err := pgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PricingQuery, *PricingGroupBy](ctx, pgb.build, pgb, pgb.build.inters, v)
}

func (pgb *PricingGroupBy) sqlScan(ctx context.Context, root *PricingQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(pgb.fns))
	for _, fn := range pgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*pgb.flds)+len(pgb.fns))
		for _, f := range *pgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*pgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := pgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// PricingSelect is the builder for selecting fields of Pricing entities.
type PricingSelect struct {
	*PricingQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ps *PricingSelect) Aggregate(fns ...AggregateFunc) *PricingSelect {
	ps.fns = append(ps.fns, fns...)
	return ps
}

// Scan applies the selector query and scans the result into the given value.
func (ps *PricingSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ps.ctx, "Select")
	if err := ps.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PricingQuery, *PricingSelect](ctx, ps.PricingQuery, ps, ps.inters, v)
}

func (ps *PricingSelect) sqlScan(ctx context.Context, root *PricingQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ps.fns))
	for _, fn := range ps.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ps.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ps.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (ps *PricingSelect) Modify(modifiers ...func(s *sql.Selector)) *PricingSelect {
	ps.modifiers = append(ps.modifiers, modifiers...)
	return ps
}
