// Code generated by ent, DO NOT EDIT.

package ent

import (
	"encoding/json"
	"fmt"
	"roofix/ent/apiuser"
	"roofix/ent/partner"
	"roofix/ent/partneractivity"
	"roofix/ent/user"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// PartnerActivity is the model entity for the PartnerActivity schema.
type PartnerActivity struct {
	config `json:"-"`
	// ID of the ent.
	ID string `json:"id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"createdAt"`
	// Description holds the value of the "description" field.
	Description string `json:"description,omitempty"`
	// any related data
	Raw map[string]interface{} `json:"raw,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the PartnerActivityQuery when eager-loading is set.
	Edges        PartnerActivityEdges `json:"edges"`
	api_user_id  *string
	partner_id   *string
	creator_id   *string
	selectValues sql.SelectValues
}

// PartnerActivityEdges holds the relations/edges for other nodes in the graph.
type PartnerActivityEdges struct {
	// Partner holds the value of the partner edge.
	Partner *Partner `json:"partner,omitempty"`
	// Creator holds the value of the creator edge.
	Creator *User `json:"creator,omitempty"`
	// CreatorAPI holds the value of the creator_api edge.
	CreatorAPI *ApiUser `json:"creator_api,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [3]bool
	// totalCount holds the count of the edges above.
	totalCount [3]map[string]int
}

// PartnerOrErr returns the Partner value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e PartnerActivityEdges) PartnerOrErr() (*Partner, error) {
	if e.loadedTypes[0] {
		if e.Partner == nil {
			// Edge was loaded but was not found.
			return nil, &NotFoundError{label: partner.Label}
		}
		return e.Partner, nil
	}
	return nil, &NotLoadedError{edge: "partner"}
}

// CreatorOrErr returns the Creator value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e PartnerActivityEdges) CreatorOrErr() (*User, error) {
	if e.loadedTypes[1] {
		if e.Creator == nil {
			// Edge was loaded but was not found.
			return nil, &NotFoundError{label: user.Label}
		}
		return e.Creator, nil
	}
	return nil, &NotLoadedError{edge: "creator"}
}

// CreatorAPIOrErr returns the CreatorAPI value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e PartnerActivityEdges) CreatorAPIOrErr() (*ApiUser, error) {
	if e.loadedTypes[2] {
		if e.CreatorAPI == nil {
			// Edge was loaded but was not found.
			return nil, &NotFoundError{label: apiuser.Label}
		}
		return e.CreatorAPI, nil
	}
	return nil, &NotLoadedError{edge: "creator_api"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*PartnerActivity) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case partneractivity.FieldRaw:
			values[i] = new([]byte)
		case partneractivity.FieldID, partneractivity.FieldDescription:
			values[i] = new(sql.NullString)
		case partneractivity.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		case partneractivity.ForeignKeys[0]: // api_user_id
			values[i] = new(sql.NullString)
		case partneractivity.ForeignKeys[1]: // partner_id
			values[i] = new(sql.NullString)
		case partneractivity.ForeignKeys[2]: // creator_id
			values[i] = new(sql.NullString)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the PartnerActivity fields.
func (pa *PartnerActivity) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case partneractivity.FieldID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value.Valid {
				pa.ID = value.String
			}
		case partneractivity.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				pa.CreatedAt = value.Time
			}
		case partneractivity.FieldDescription:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field description", values[i])
			} else if value.Valid {
				pa.Description = value.String
			}
		case partneractivity.FieldRaw:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field raw", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &pa.Raw); err != nil {
					return fmt.Errorf("unmarshal field raw: %w", err)
				}
			}
		case partneractivity.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field api_user_id", values[i])
			} else if value.Valid {
				pa.api_user_id = new(string)
				*pa.api_user_id = value.String
			}
		case partneractivity.ForeignKeys[1]:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field partner_id", values[i])
			} else if value.Valid {
				pa.partner_id = new(string)
				*pa.partner_id = value.String
			}
		case partneractivity.ForeignKeys[2]:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field creator_id", values[i])
			} else if value.Valid {
				pa.creator_id = new(string)
				*pa.creator_id = value.String
			}
		default:
			pa.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the PartnerActivity.
// This includes values selected through modifiers, order, etc.
func (pa *PartnerActivity) Value(name string) (ent.Value, error) {
	return pa.selectValues.Get(name)
}

// QueryPartner queries the "partner" edge of the PartnerActivity entity.
func (pa *PartnerActivity) QueryPartner() *PartnerQuery {
	return NewPartnerActivityClient(pa.config).QueryPartner(pa)
}

// QueryCreator queries the "creator" edge of the PartnerActivity entity.
func (pa *PartnerActivity) QueryCreator() *UserQuery {
	return NewPartnerActivityClient(pa.config).QueryCreator(pa)
}

// QueryCreatorAPI queries the "creator_api" edge of the PartnerActivity entity.
func (pa *PartnerActivity) QueryCreatorAPI() *ApiUserQuery {
	return NewPartnerActivityClient(pa.config).QueryCreatorAPI(pa)
}

// Update returns a builder for updating this PartnerActivity.
// Note that you need to call PartnerActivity.Unwrap() before calling this method if this PartnerActivity
// was returned from a transaction, and the transaction was committed or rolled back.
func (pa *PartnerActivity) Update() *PartnerActivityUpdateOne {
	return NewPartnerActivityClient(pa.config).UpdateOne(pa)
}

// Unwrap unwraps the PartnerActivity entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (pa *PartnerActivity) Unwrap() *PartnerActivity {
	_tx, ok := pa.config.driver.(*txDriver)
	if !ok {
		panic("ent: PartnerActivity is not a transactional entity")
	}
	pa.config.driver = _tx.drv
	return pa
}

// String implements the fmt.Stringer.
func (pa *PartnerActivity) String() string {
	var builder strings.Builder
	builder.WriteString("PartnerActivity(")
	builder.WriteString(fmt.Sprintf("id=%v, ", pa.ID))
	builder.WriteString("created_at=")
	builder.WriteString(pa.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("description=")
	builder.WriteString(pa.Description)
	builder.WriteString(", ")
	builder.WriteString("raw=")
	builder.WriteString(fmt.Sprintf("%v", pa.Raw))
	builder.WriteByte(')')
	return builder.String()
}

// PartnerActivities is a parsable slice of PartnerActivity.
type PartnerActivities []*PartnerActivity
