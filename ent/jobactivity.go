// Code generated by ent, DO NOT EDIT.

package ent

import (
	"encoding/json"
	"fmt"
	"roofix/ent/apiuser"
	"roofix/ent/job"
	"roofix/ent/jobactivity"
	"roofix/ent/user"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// JobActivity is the model entity for the JobActivity schema.
type JobActivity struct {
	config `json:"-"`
	// ID of the ent.
	ID string `json:"id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"createdAt"`
	// Description holds the value of the "description" field.
	Description string `json:"description,omitempty"`
	// any related data
	Raw map[string]interface{} `json:"raw,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the JobActivityQuery when eager-loading is set.
	Edges        JobActivityEdges `json:"edges"`
	api_user_id  *string
	job_id       *string
	creator_id   *string
	selectValues sql.SelectValues
}

// JobActivityEdges holds the relations/edges for other nodes in the graph.
type JobActivityEdges struct {
	// Job holds the value of the job edge.
	Job *Job `json:"job,omitempty"`
	// Creator holds the value of the creator edge.
	Creator *User `json:"creator,omitempty"`
	// CreatorAPI holds the value of the creator_api edge.
	CreatorAPI *ApiUser `json:"creator_api,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [3]bool
	// totalCount holds the count of the edges above.
	totalCount [3]map[string]int
}

// JobOrErr returns the Job value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e JobActivityEdges) JobOrErr() (*Job, error) {
	if e.loadedTypes[0] {
		if e.Job == nil {
			// Edge was loaded but was not found.
			return nil, &NotFoundError{label: job.Label}
		}
		return e.Job, nil
	}
	return nil, &NotLoadedError{edge: "job"}
}

// CreatorOrErr returns the Creator value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e JobActivityEdges) CreatorOrErr() (*User, error) {
	if e.loadedTypes[1] {
		if e.Creator == nil {
			// Edge was loaded but was not found.
			return nil, &NotFoundError{label: user.Label}
		}
		return e.Creator, nil
	}
	return nil, &NotLoadedError{edge: "creator"}
}

// CreatorAPIOrErr returns the CreatorAPI value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e JobActivityEdges) CreatorAPIOrErr() (*ApiUser, error) {
	if e.loadedTypes[2] {
		if e.CreatorAPI == nil {
			// Edge was loaded but was not found.
			return nil, &NotFoundError{label: apiuser.Label}
		}
		return e.CreatorAPI, nil
	}
	return nil, &NotLoadedError{edge: "creator_api"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*JobActivity) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case jobactivity.FieldRaw:
			values[i] = new([]byte)
		case jobactivity.FieldID, jobactivity.FieldDescription:
			values[i] = new(sql.NullString)
		case jobactivity.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		case jobactivity.ForeignKeys[0]: // api_user_id
			values[i] = new(sql.NullString)
		case jobactivity.ForeignKeys[1]: // job_id
			values[i] = new(sql.NullString)
		case jobactivity.ForeignKeys[2]: // creator_id
			values[i] = new(sql.NullString)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the JobActivity fields.
func (ja *JobActivity) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case jobactivity.FieldID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value.Valid {
				ja.ID = value.String
			}
		case jobactivity.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				ja.CreatedAt = value.Time
			}
		case jobactivity.FieldDescription:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field description", values[i])
			} else if value.Valid {
				ja.Description = value.String
			}
		case jobactivity.FieldRaw:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field raw", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &ja.Raw); err != nil {
					return fmt.Errorf("unmarshal field raw: %w", err)
				}
			}
		case jobactivity.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field api_user_id", values[i])
			} else if value.Valid {
				ja.api_user_id = new(string)
				*ja.api_user_id = value.String
			}
		case jobactivity.ForeignKeys[1]:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field job_id", values[i])
			} else if value.Valid {
				ja.job_id = new(string)
				*ja.job_id = value.String
			}
		case jobactivity.ForeignKeys[2]:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field creator_id", values[i])
			} else if value.Valid {
				ja.creator_id = new(string)
				*ja.creator_id = value.String
			}
		default:
			ja.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the JobActivity.
// This includes values selected through modifiers, order, etc.
func (ja *JobActivity) Value(name string) (ent.Value, error) {
	return ja.selectValues.Get(name)
}

// QueryJob queries the "job" edge of the JobActivity entity.
func (ja *JobActivity) QueryJob() *JobQuery {
	return NewJobActivityClient(ja.config).QueryJob(ja)
}

// QueryCreator queries the "creator" edge of the JobActivity entity.
func (ja *JobActivity) QueryCreator() *UserQuery {
	return NewJobActivityClient(ja.config).QueryCreator(ja)
}

// QueryCreatorAPI queries the "creator_api" edge of the JobActivity entity.
func (ja *JobActivity) QueryCreatorAPI() *ApiUserQuery {
	return NewJobActivityClient(ja.config).QueryCreatorAPI(ja)
}

// Update returns a builder for updating this JobActivity.
// Note that you need to call JobActivity.Unwrap() before calling this method if this JobActivity
// was returned from a transaction, and the transaction was committed or rolled back.
func (ja *JobActivity) Update() *JobActivityUpdateOne {
	return NewJobActivityClient(ja.config).UpdateOne(ja)
}

// Unwrap unwraps the JobActivity entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (ja *JobActivity) Unwrap() *JobActivity {
	_tx, ok := ja.config.driver.(*txDriver)
	if !ok {
		panic("ent: JobActivity is not a transactional entity")
	}
	ja.config.driver = _tx.drv
	return ja
}

// String implements the fmt.Stringer.
func (ja *JobActivity) String() string {
	var builder strings.Builder
	builder.WriteString("JobActivity(")
	builder.WriteString(fmt.Sprintf("id=%v, ", ja.ID))
	builder.WriteString("created_at=")
	builder.WriteString(ja.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("description=")
	builder.WriteString(ja.Description)
	builder.WriteString(", ")
	builder.WriteString("raw=")
	builder.WriteString(fmt.Sprintf("%v", ja.Raw))
	builder.WriteByte(')')
	return builder.String()
}

// JobActivities is a parsable slice of JobActivity.
type JobActivities []*JobActivity
