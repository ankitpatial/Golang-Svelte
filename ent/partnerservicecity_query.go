// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"roofix/ent/partner"
	"roofix/ent/partnerservicecity"
	"roofix/ent/predicate"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// PartnerServiceCityQuery is the builder for querying PartnerServiceCity entities.
type PartnerServiceCityQuery struct {
	config
	ctx         *QueryContext
	order       []partnerservicecity.OrderOption
	inters      []Interceptor
	predicates  []predicate.PartnerServiceCity
	withPartner *PartnerQuery
	withFKs     bool
	loadTotal   []func(context.Context, []*PartnerServiceCity) error
	modifiers   []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the PartnerServiceCityQuery builder.
func (pscq *PartnerServiceCityQuery) Where(ps ...predicate.PartnerServiceCity) *PartnerServiceCityQuery {
	pscq.predicates = append(pscq.predicates, ps...)
	return pscq
}

// Limit the number of records to be returned by this query.
func (pscq *PartnerServiceCityQuery) Limit(limit int) *PartnerServiceCityQuery {
	pscq.ctx.Limit = &limit
	return pscq
}

// Offset to start from.
func (pscq *PartnerServiceCityQuery) Offset(offset int) *PartnerServiceCityQuery {
	pscq.ctx.Offset = &offset
	return pscq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (pscq *PartnerServiceCityQuery) Unique(unique bool) *PartnerServiceCityQuery {
	pscq.ctx.Unique = &unique
	return pscq
}

// Order specifies how the records should be ordered.
func (pscq *PartnerServiceCityQuery) Order(o ...partnerservicecity.OrderOption) *PartnerServiceCityQuery {
	pscq.order = append(pscq.order, o...)
	return pscq
}

// QueryPartner chains the current query on the "partner" edge.
func (pscq *PartnerServiceCityQuery) QueryPartner() *PartnerQuery {
	query := (&PartnerClient{config: pscq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := pscq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := pscq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(partnerservicecity.Table, partnerservicecity.FieldID, selector),
			sqlgraph.To(partner.Table, partner.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, partnerservicecity.PartnerTable, partnerservicecity.PartnerColumn),
		)
		fromU = sqlgraph.SetNeighbors(pscq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first PartnerServiceCity entity from the query.
// Returns a *NotFoundError when no PartnerServiceCity was found.
func (pscq *PartnerServiceCityQuery) First(ctx context.Context) (*PartnerServiceCity, error) {
	nodes, err := pscq.Limit(1).All(setContextOp(ctx, pscq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{partnerservicecity.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (pscq *PartnerServiceCityQuery) FirstX(ctx context.Context) *PartnerServiceCity {
	node, err := pscq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first PartnerServiceCity ID from the query.
// Returns a *NotFoundError when no PartnerServiceCity ID was found.
func (pscq *PartnerServiceCityQuery) FirstID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = pscq.Limit(1).IDs(setContextOp(ctx, pscq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{partnerservicecity.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (pscq *PartnerServiceCityQuery) FirstIDX(ctx context.Context) string {
	id, err := pscq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single PartnerServiceCity entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one PartnerServiceCity entity is found.
// Returns a *NotFoundError when no PartnerServiceCity entities are found.
func (pscq *PartnerServiceCityQuery) Only(ctx context.Context) (*PartnerServiceCity, error) {
	nodes, err := pscq.Limit(2).All(setContextOp(ctx, pscq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{partnerservicecity.Label}
	default:
		return nil, &NotSingularError{partnerservicecity.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (pscq *PartnerServiceCityQuery) OnlyX(ctx context.Context) *PartnerServiceCity {
	node, err := pscq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only PartnerServiceCity ID in the query.
// Returns a *NotSingularError when more than one PartnerServiceCity ID is found.
// Returns a *NotFoundError when no entities are found.
func (pscq *PartnerServiceCityQuery) OnlyID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = pscq.Limit(2).IDs(setContextOp(ctx, pscq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{partnerservicecity.Label}
	default:
		err = &NotSingularError{partnerservicecity.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (pscq *PartnerServiceCityQuery) OnlyIDX(ctx context.Context) string {
	id, err := pscq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of PartnerServiceCities.
func (pscq *PartnerServiceCityQuery) All(ctx context.Context) ([]*PartnerServiceCity, error) {
	ctx = setContextOp(ctx, pscq.ctx, "All")
	if err := pscq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*PartnerServiceCity, *PartnerServiceCityQuery]()
	return withInterceptors[[]*PartnerServiceCity](ctx, pscq, qr, pscq.inters)
}

// AllX is like All, but panics if an error occurs.
func (pscq *PartnerServiceCityQuery) AllX(ctx context.Context) []*PartnerServiceCity {
	nodes, err := pscq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of PartnerServiceCity IDs.
func (pscq *PartnerServiceCityQuery) IDs(ctx context.Context) (ids []string, err error) {
	if pscq.ctx.Unique == nil && pscq.path != nil {
		pscq.Unique(true)
	}
	ctx = setContextOp(ctx, pscq.ctx, "IDs")
	if err = pscq.Select(partnerservicecity.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (pscq *PartnerServiceCityQuery) IDsX(ctx context.Context) []string {
	ids, err := pscq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (pscq *PartnerServiceCityQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, pscq.ctx, "Count")
	if err := pscq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, pscq, querierCount[*PartnerServiceCityQuery](), pscq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (pscq *PartnerServiceCityQuery) CountX(ctx context.Context) int {
	count, err := pscq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (pscq *PartnerServiceCityQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, pscq.ctx, "Exist")
	switch _, err := pscq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (pscq *PartnerServiceCityQuery) ExistX(ctx context.Context) bool {
	exist, err := pscq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the PartnerServiceCityQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (pscq *PartnerServiceCityQuery) Clone() *PartnerServiceCityQuery {
	if pscq == nil {
		return nil
	}
	return &PartnerServiceCityQuery{
		config:      pscq.config,
		ctx:         pscq.ctx.Clone(),
		order:       append([]partnerservicecity.OrderOption{}, pscq.order...),
		inters:      append([]Interceptor{}, pscq.inters...),
		predicates:  append([]predicate.PartnerServiceCity{}, pscq.predicates...),
		withPartner: pscq.withPartner.Clone(),
		// clone intermediate query.
		sql:  pscq.sql.Clone(),
		path: pscq.path,
	}
}

// WithPartner tells the query-builder to eager-load the nodes that are connected to
// the "partner" edge. The optional arguments are used to configure the query builder of the edge.
func (pscq *PartnerServiceCityQuery) WithPartner(opts ...func(*PartnerQuery)) *PartnerServiceCityQuery {
	query := (&PartnerClient{config: pscq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	pscq.withPartner = query
	return pscq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"createdAt"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.PartnerServiceCity.Query().
//		GroupBy(partnerservicecity.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (pscq *PartnerServiceCityQuery) GroupBy(field string, fields ...string) *PartnerServiceCityGroupBy {
	pscq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &PartnerServiceCityGroupBy{build: pscq}
	grbuild.flds = &pscq.ctx.Fields
	grbuild.label = partnerservicecity.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"createdAt"`
//	}
//
//	client.PartnerServiceCity.Query().
//		Select(partnerservicecity.FieldCreatedAt).
//		Scan(ctx, &v)
func (pscq *PartnerServiceCityQuery) Select(fields ...string) *PartnerServiceCitySelect {
	pscq.ctx.Fields = append(pscq.ctx.Fields, fields...)
	sbuild := &PartnerServiceCitySelect{PartnerServiceCityQuery: pscq}
	sbuild.label = partnerservicecity.Label
	sbuild.flds, sbuild.scan = &pscq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a PartnerServiceCitySelect configured with the given aggregations.
func (pscq *PartnerServiceCityQuery) Aggregate(fns ...AggregateFunc) *PartnerServiceCitySelect {
	return pscq.Select().Aggregate(fns...)
}

func (pscq *PartnerServiceCityQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range pscq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, pscq); err != nil {
				return err
			}
		}
	}
	for _, f := range pscq.ctx.Fields {
		if !partnerservicecity.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if pscq.path != nil {
		prev, err := pscq.path(ctx)
		if err != nil {
			return err
		}
		pscq.sql = prev
	}
	return nil
}

func (pscq *PartnerServiceCityQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*PartnerServiceCity, error) {
	var (
		nodes       = []*PartnerServiceCity{}
		withFKs     = pscq.withFKs
		_spec       = pscq.querySpec()
		loadedTypes = [1]bool{
			pscq.withPartner != nil,
		}
	)
	if pscq.withPartner != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, partnerservicecity.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*PartnerServiceCity).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &PartnerServiceCity{config: pscq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(pscq.modifiers) > 0 {
		_spec.Modifiers = pscq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, pscq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := pscq.withPartner; query != nil {
		if err := pscq.loadPartner(ctx, query, nodes, nil,
			func(n *PartnerServiceCity, e *Partner) { n.Edges.Partner = e }); err != nil {
			return nil, err
		}
	}
	for i := range pscq.loadTotal {
		if err := pscq.loadTotal[i](ctx, nodes); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (pscq *PartnerServiceCityQuery) loadPartner(ctx context.Context, query *PartnerQuery, nodes []*PartnerServiceCity, init func(*PartnerServiceCity), assign func(*PartnerServiceCity, *Partner)) error {
	ids := make([]string, 0, len(nodes))
	nodeids := make(map[string][]*PartnerServiceCity)
	for i := range nodes {
		if nodes[i].partner_id == nil {
			continue
		}
		fk := *nodes[i].partner_id
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(partner.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "partner_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (pscq *PartnerServiceCityQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := pscq.querySpec()
	if len(pscq.modifiers) > 0 {
		_spec.Modifiers = pscq.modifiers
	}
	_spec.Node.Columns = pscq.ctx.Fields
	if len(pscq.ctx.Fields) > 0 {
		_spec.Unique = pscq.ctx.Unique != nil && *pscq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, pscq.driver, _spec)
}

func (pscq *PartnerServiceCityQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(partnerservicecity.Table, partnerservicecity.Columns, sqlgraph.NewFieldSpec(partnerservicecity.FieldID, field.TypeString))
	_spec.From = pscq.sql
	if unique := pscq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if pscq.path != nil {
		_spec.Unique = true
	}
	if fields := pscq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, partnerservicecity.FieldID)
		for i := range fields {
			if fields[i] != partnerservicecity.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := pscq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := pscq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := pscq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := pscq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (pscq *PartnerServiceCityQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(pscq.driver.Dialect())
	t1 := builder.Table(partnerservicecity.Table)
	columns := pscq.ctx.Fields
	if len(columns) == 0 {
		columns = partnerservicecity.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if pscq.sql != nil {
		selector = pscq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if pscq.ctx.Unique != nil && *pscq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range pscq.modifiers {
		m(selector)
	}
	for _, p := range pscq.predicates {
		p(selector)
	}
	for _, p := range pscq.order {
		p(selector)
	}
	if offset := pscq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := pscq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// Modify adds a query modifier for attaching custom logic to queries.
func (pscq *PartnerServiceCityQuery) Modify(modifiers ...func(s *sql.Selector)) *PartnerServiceCitySelect {
	pscq.modifiers = append(pscq.modifiers, modifiers...)
	return pscq.Select()
}

// PartnerServiceCityGroupBy is the group-by builder for PartnerServiceCity entities.
type PartnerServiceCityGroupBy struct {
	selector
	build *PartnerServiceCityQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (pscgb *PartnerServiceCityGroupBy) Aggregate(fns ...AggregateFunc) *PartnerServiceCityGroupBy {
	pscgb.fns = append(pscgb.fns, fns...)
	return pscgb
}

// Scan applies the selector query and scans the result into the given value.
func (pscgb *PartnerServiceCityGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, pscgb.build.ctx, "GroupBy")
	if err := pscgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PartnerServiceCityQuery, *PartnerServiceCityGroupBy](ctx, pscgb.build, pscgb, pscgb.build.inters, v)
}

func (pscgb *PartnerServiceCityGroupBy) sqlScan(ctx context.Context, root *PartnerServiceCityQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(pscgb.fns))
	for _, fn := range pscgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*pscgb.flds)+len(pscgb.fns))
		for _, f := range *pscgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*pscgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := pscgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// PartnerServiceCitySelect is the builder for selecting fields of PartnerServiceCity entities.
type PartnerServiceCitySelect struct {
	*PartnerServiceCityQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (pscs *PartnerServiceCitySelect) Aggregate(fns ...AggregateFunc) *PartnerServiceCitySelect {
	pscs.fns = append(pscs.fns, fns...)
	return pscs
}

// Scan applies the selector query and scans the result into the given value.
func (pscs *PartnerServiceCitySelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, pscs.ctx, "Select")
	if err := pscs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PartnerServiceCityQuery, *PartnerServiceCitySelect](ctx, pscs.PartnerServiceCityQuery, pscs, pscs.inters, v)
}

func (pscs *PartnerServiceCitySelect) sqlScan(ctx context.Context, root *PartnerServiceCityQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(pscs.fns))
	for _, fn := range pscs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*pscs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := pscs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (pscs *PartnerServiceCitySelect) Modify(modifiers ...func(s *sql.Selector)) *PartnerServiceCitySelect {
	pscs.modifiers = append(pscs.modifiers, modifiers...)
	return pscs
}
