// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"roofix/ent/installationjob"
	"roofix/ent/installationjobitem"
	"roofix/ent/predicate"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// InstallationJobItemQuery is the builder for querying InstallationJobItem entities.
type InstallationJobItemQuery struct {
	config
	ctx        *QueryContext
	order      []installationjobitem.OrderOption
	inters     []Interceptor
	predicates []predicate.InstallationJobItem
	withJob    *InstallationJobQuery
	withFKs    bool
	loadTotal  []func(context.Context, []*InstallationJobItem) error
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the InstallationJobItemQuery builder.
func (ijiq *InstallationJobItemQuery) Where(ps ...predicate.InstallationJobItem) *InstallationJobItemQuery {
	ijiq.predicates = append(ijiq.predicates, ps...)
	return ijiq
}

// Limit the number of records to be returned by this query.
func (ijiq *InstallationJobItemQuery) Limit(limit int) *InstallationJobItemQuery {
	ijiq.ctx.Limit = &limit
	return ijiq
}

// Offset to start from.
func (ijiq *InstallationJobItemQuery) Offset(offset int) *InstallationJobItemQuery {
	ijiq.ctx.Offset = &offset
	return ijiq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (ijiq *InstallationJobItemQuery) Unique(unique bool) *InstallationJobItemQuery {
	ijiq.ctx.Unique = &unique
	return ijiq
}

// Order specifies how the records should be ordered.
func (ijiq *InstallationJobItemQuery) Order(o ...installationjobitem.OrderOption) *InstallationJobItemQuery {
	ijiq.order = append(ijiq.order, o...)
	return ijiq
}

// QueryJob chains the current query on the "job" edge.
func (ijiq *InstallationJobItemQuery) QueryJob() *InstallationJobQuery {
	query := (&InstallationJobClient{config: ijiq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := ijiq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := ijiq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(installationjobitem.Table, installationjobitem.FieldID, selector),
			sqlgraph.To(installationjob.Table, installationjob.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, installationjobitem.JobTable, installationjobitem.JobColumn),
		)
		fromU = sqlgraph.SetNeighbors(ijiq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first InstallationJobItem entity from the query.
// Returns a *NotFoundError when no InstallationJobItem was found.
func (ijiq *InstallationJobItemQuery) First(ctx context.Context) (*InstallationJobItem, error) {
	nodes, err := ijiq.Limit(1).All(setContextOp(ctx, ijiq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{installationjobitem.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (ijiq *InstallationJobItemQuery) FirstX(ctx context.Context) *InstallationJobItem {
	node, err := ijiq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first InstallationJobItem ID from the query.
// Returns a *NotFoundError when no InstallationJobItem ID was found.
func (ijiq *InstallationJobItemQuery) FirstID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = ijiq.Limit(1).IDs(setContextOp(ctx, ijiq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{installationjobitem.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (ijiq *InstallationJobItemQuery) FirstIDX(ctx context.Context) string {
	id, err := ijiq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single InstallationJobItem entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one InstallationJobItem entity is found.
// Returns a *NotFoundError when no InstallationJobItem entities are found.
func (ijiq *InstallationJobItemQuery) Only(ctx context.Context) (*InstallationJobItem, error) {
	nodes, err := ijiq.Limit(2).All(setContextOp(ctx, ijiq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{installationjobitem.Label}
	default:
		return nil, &NotSingularError{installationjobitem.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (ijiq *InstallationJobItemQuery) OnlyX(ctx context.Context) *InstallationJobItem {
	node, err := ijiq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only InstallationJobItem ID in the query.
// Returns a *NotSingularError when more than one InstallationJobItem ID is found.
// Returns a *NotFoundError when no entities are found.
func (ijiq *InstallationJobItemQuery) OnlyID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = ijiq.Limit(2).IDs(setContextOp(ctx, ijiq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{installationjobitem.Label}
	default:
		err = &NotSingularError{installationjobitem.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (ijiq *InstallationJobItemQuery) OnlyIDX(ctx context.Context) string {
	id, err := ijiq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of InstallationJobItems.
func (ijiq *InstallationJobItemQuery) All(ctx context.Context) ([]*InstallationJobItem, error) {
	ctx = setContextOp(ctx, ijiq.ctx, "All")
	if err := ijiq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*InstallationJobItem, *InstallationJobItemQuery]()
	return withInterceptors[[]*InstallationJobItem](ctx, ijiq, qr, ijiq.inters)
}

// AllX is like All, but panics if an error occurs.
func (ijiq *InstallationJobItemQuery) AllX(ctx context.Context) []*InstallationJobItem {
	nodes, err := ijiq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of InstallationJobItem IDs.
func (ijiq *InstallationJobItemQuery) IDs(ctx context.Context) (ids []string, err error) {
	if ijiq.ctx.Unique == nil && ijiq.path != nil {
		ijiq.Unique(true)
	}
	ctx = setContextOp(ctx, ijiq.ctx, "IDs")
	if err = ijiq.Select(installationjobitem.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (ijiq *InstallationJobItemQuery) IDsX(ctx context.Context) []string {
	ids, err := ijiq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (ijiq *InstallationJobItemQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, ijiq.ctx, "Count")
	if err := ijiq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, ijiq, querierCount[*InstallationJobItemQuery](), ijiq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (ijiq *InstallationJobItemQuery) CountX(ctx context.Context) int {
	count, err := ijiq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (ijiq *InstallationJobItemQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, ijiq.ctx, "Exist")
	switch _, err := ijiq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (ijiq *InstallationJobItemQuery) ExistX(ctx context.Context) bool {
	exist, err := ijiq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the InstallationJobItemQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (ijiq *InstallationJobItemQuery) Clone() *InstallationJobItemQuery {
	if ijiq == nil {
		return nil
	}
	return &InstallationJobItemQuery{
		config:     ijiq.config,
		ctx:        ijiq.ctx.Clone(),
		order:      append([]installationjobitem.OrderOption{}, ijiq.order...),
		inters:     append([]Interceptor{}, ijiq.inters...),
		predicates: append([]predicate.InstallationJobItem{}, ijiq.predicates...),
		withJob:    ijiq.withJob.Clone(),
		// clone intermediate query.
		sql:  ijiq.sql.Clone(),
		path: ijiq.path,
	}
}

// WithJob tells the query-builder to eager-load the nodes that are connected to
// the "job" edge. The optional arguments are used to configure the query builder of the edge.
func (ijiq *InstallationJobItemQuery) WithJob(opts ...func(*InstallationJobQuery)) *InstallationJobItemQuery {
	query := (&InstallationJobClient{config: ijiq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	ijiq.withJob = query
	return ijiq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Name string `json:"name,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.InstallationJobItem.Query().
//		GroupBy(installationjobitem.FieldName).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (ijiq *InstallationJobItemQuery) GroupBy(field string, fields ...string) *InstallationJobItemGroupBy {
	ijiq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &InstallationJobItemGroupBy{build: ijiq}
	grbuild.flds = &ijiq.ctx.Fields
	grbuild.label = installationjobitem.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Name string `json:"name,omitempty"`
//	}
//
//	client.InstallationJobItem.Query().
//		Select(installationjobitem.FieldName).
//		Scan(ctx, &v)
func (ijiq *InstallationJobItemQuery) Select(fields ...string) *InstallationJobItemSelect {
	ijiq.ctx.Fields = append(ijiq.ctx.Fields, fields...)
	sbuild := &InstallationJobItemSelect{InstallationJobItemQuery: ijiq}
	sbuild.label = installationjobitem.Label
	sbuild.flds, sbuild.scan = &ijiq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a InstallationJobItemSelect configured with the given aggregations.
func (ijiq *InstallationJobItemQuery) Aggregate(fns ...AggregateFunc) *InstallationJobItemSelect {
	return ijiq.Select().Aggregate(fns...)
}

func (ijiq *InstallationJobItemQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range ijiq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, ijiq); err != nil {
				return err
			}
		}
	}
	for _, f := range ijiq.ctx.Fields {
		if !installationjobitem.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if ijiq.path != nil {
		prev, err := ijiq.path(ctx)
		if err != nil {
			return err
		}
		ijiq.sql = prev
	}
	return nil
}

func (ijiq *InstallationJobItemQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*InstallationJobItem, error) {
	var (
		nodes       = []*InstallationJobItem{}
		withFKs     = ijiq.withFKs
		_spec       = ijiq.querySpec()
		loadedTypes = [1]bool{
			ijiq.withJob != nil,
		}
	)
	if ijiq.withJob != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, installationjobitem.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*InstallationJobItem).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &InstallationJobItem{config: ijiq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(ijiq.modifiers) > 0 {
		_spec.Modifiers = ijiq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, ijiq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := ijiq.withJob; query != nil {
		if err := ijiq.loadJob(ctx, query, nodes, nil,
			func(n *InstallationJobItem, e *InstallationJob) { n.Edges.Job = e }); err != nil {
			return nil, err
		}
	}
	for i := range ijiq.loadTotal {
		if err := ijiq.loadTotal[i](ctx, nodes); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (ijiq *InstallationJobItemQuery) loadJob(ctx context.Context, query *InstallationJobQuery, nodes []*InstallationJobItem, init func(*InstallationJobItem), assign func(*InstallationJobItem, *InstallationJob)) error {
	ids := make([]string, 0, len(nodes))
	nodeids := make(map[string][]*InstallationJobItem)
	for i := range nodes {
		if nodes[i].job_id == nil {
			continue
		}
		fk := *nodes[i].job_id
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(installationjob.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "job_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (ijiq *InstallationJobItemQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := ijiq.querySpec()
	if len(ijiq.modifiers) > 0 {
		_spec.Modifiers = ijiq.modifiers
	}
	_spec.Node.Columns = ijiq.ctx.Fields
	if len(ijiq.ctx.Fields) > 0 {
		_spec.Unique = ijiq.ctx.Unique != nil && *ijiq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, ijiq.driver, _spec)
}

func (ijiq *InstallationJobItemQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(installationjobitem.Table, installationjobitem.Columns, sqlgraph.NewFieldSpec(installationjobitem.FieldID, field.TypeString))
	_spec.From = ijiq.sql
	if unique := ijiq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if ijiq.path != nil {
		_spec.Unique = true
	}
	if fields := ijiq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, installationjobitem.FieldID)
		for i := range fields {
			if fields[i] != installationjobitem.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := ijiq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := ijiq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := ijiq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := ijiq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (ijiq *InstallationJobItemQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(ijiq.driver.Dialect())
	t1 := builder.Table(installationjobitem.Table)
	columns := ijiq.ctx.Fields
	if len(columns) == 0 {
		columns = installationjobitem.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if ijiq.sql != nil {
		selector = ijiq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if ijiq.ctx.Unique != nil && *ijiq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range ijiq.modifiers {
		m(selector)
	}
	for _, p := range ijiq.predicates {
		p(selector)
	}
	for _, p := range ijiq.order {
		p(selector)
	}
	if offset := ijiq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := ijiq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// Modify adds a query modifier for attaching custom logic to queries.
func (ijiq *InstallationJobItemQuery) Modify(modifiers ...func(s *sql.Selector)) *InstallationJobItemSelect {
	ijiq.modifiers = append(ijiq.modifiers, modifiers...)
	return ijiq.Select()
}

// InstallationJobItemGroupBy is the group-by builder for InstallationJobItem entities.
type InstallationJobItemGroupBy struct {
	selector
	build *InstallationJobItemQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (ijigb *InstallationJobItemGroupBy) Aggregate(fns ...AggregateFunc) *InstallationJobItemGroupBy {
	ijigb.fns = append(ijigb.fns, fns...)
	return ijigb
}

// Scan applies the selector query and scans the result into the given value.
func (ijigb *InstallationJobItemGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ijigb.build.ctx, "GroupBy")
	if err := ijigb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*InstallationJobItemQuery, *InstallationJobItemGroupBy](ctx, ijigb.build, ijigb, ijigb.build.inters, v)
}

func (ijigb *InstallationJobItemGroupBy) sqlScan(ctx context.Context, root *InstallationJobItemQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(ijigb.fns))
	for _, fn := range ijigb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*ijigb.flds)+len(ijigb.fns))
		for _, f := range *ijigb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*ijigb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ijigb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// InstallationJobItemSelect is the builder for selecting fields of InstallationJobItem entities.
type InstallationJobItemSelect struct {
	*InstallationJobItemQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ijis *InstallationJobItemSelect) Aggregate(fns ...AggregateFunc) *InstallationJobItemSelect {
	ijis.fns = append(ijis.fns, fns...)
	return ijis
}

// Scan applies the selector query and scans the result into the given value.
func (ijis *InstallationJobItemSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ijis.ctx, "Select")
	if err := ijis.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*InstallationJobItemQuery, *InstallationJobItemSelect](ctx, ijis.InstallationJobItemQuery, ijis, ijis.inters, v)
}

func (ijis *InstallationJobItemSelect) sqlScan(ctx context.Context, root *InstallationJobItemQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ijis.fns))
	for _, fn := range ijis.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ijis.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ijis.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (ijis *InstallationJobItemSelect) Modify(modifiers ...func(s *sql.Selector)) *InstallationJobItemSelect {
	ijis.modifiers = append(ijis.modifiers, modifiers...)
	return ijis
}
