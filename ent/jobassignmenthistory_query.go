// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"roofix/ent/job"
	"roofix/ent/jobassignmenthistory"
	"roofix/ent/partner"
	"roofix/ent/predicate"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// JobAssignmentHistoryQuery is the builder for querying JobAssignmentHistory entities.
type JobAssignmentHistoryQuery struct {
	config
	ctx         *QueryContext
	order       []jobassignmenthistory.OrderOption
	inters      []Interceptor
	predicates  []predicate.JobAssignmentHistory
	withJob     *JobQuery
	withPartner *PartnerQuery
	withFKs     bool
	loadTotal   []func(context.Context, []*JobAssignmentHistory) error
	modifiers   []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the JobAssignmentHistoryQuery builder.
func (jahq *JobAssignmentHistoryQuery) Where(ps ...predicate.JobAssignmentHistory) *JobAssignmentHistoryQuery {
	jahq.predicates = append(jahq.predicates, ps...)
	return jahq
}

// Limit the number of records to be returned by this query.
func (jahq *JobAssignmentHistoryQuery) Limit(limit int) *JobAssignmentHistoryQuery {
	jahq.ctx.Limit = &limit
	return jahq
}

// Offset to start from.
func (jahq *JobAssignmentHistoryQuery) Offset(offset int) *JobAssignmentHistoryQuery {
	jahq.ctx.Offset = &offset
	return jahq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (jahq *JobAssignmentHistoryQuery) Unique(unique bool) *JobAssignmentHistoryQuery {
	jahq.ctx.Unique = &unique
	return jahq
}

// Order specifies how the records should be ordered.
func (jahq *JobAssignmentHistoryQuery) Order(o ...jobassignmenthistory.OrderOption) *JobAssignmentHistoryQuery {
	jahq.order = append(jahq.order, o...)
	return jahq
}

// QueryJob chains the current query on the "job" edge.
func (jahq *JobAssignmentHistoryQuery) QueryJob() *JobQuery {
	query := (&JobClient{config: jahq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := jahq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := jahq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(jobassignmenthistory.Table, jobassignmenthistory.FieldID, selector),
			sqlgraph.To(job.Table, job.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, jobassignmenthistory.JobTable, jobassignmenthistory.JobColumn),
		)
		fromU = sqlgraph.SetNeighbors(jahq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryPartner chains the current query on the "partner" edge.
func (jahq *JobAssignmentHistoryQuery) QueryPartner() *PartnerQuery {
	query := (&PartnerClient{config: jahq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := jahq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := jahq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(jobassignmenthistory.Table, jobassignmenthistory.FieldID, selector),
			sqlgraph.To(partner.Table, partner.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, jobassignmenthistory.PartnerTable, jobassignmenthistory.PartnerColumn),
		)
		fromU = sqlgraph.SetNeighbors(jahq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first JobAssignmentHistory entity from the query.
// Returns a *NotFoundError when no JobAssignmentHistory was found.
func (jahq *JobAssignmentHistoryQuery) First(ctx context.Context) (*JobAssignmentHistory, error) {
	nodes, err := jahq.Limit(1).All(setContextOp(ctx, jahq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{jobassignmenthistory.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (jahq *JobAssignmentHistoryQuery) FirstX(ctx context.Context) *JobAssignmentHistory {
	node, err := jahq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first JobAssignmentHistory ID from the query.
// Returns a *NotFoundError when no JobAssignmentHistory ID was found.
func (jahq *JobAssignmentHistoryQuery) FirstID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = jahq.Limit(1).IDs(setContextOp(ctx, jahq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{jobassignmenthistory.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (jahq *JobAssignmentHistoryQuery) FirstIDX(ctx context.Context) string {
	id, err := jahq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single JobAssignmentHistory entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one JobAssignmentHistory entity is found.
// Returns a *NotFoundError when no JobAssignmentHistory entities are found.
func (jahq *JobAssignmentHistoryQuery) Only(ctx context.Context) (*JobAssignmentHistory, error) {
	nodes, err := jahq.Limit(2).All(setContextOp(ctx, jahq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{jobassignmenthistory.Label}
	default:
		return nil, &NotSingularError{jobassignmenthistory.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (jahq *JobAssignmentHistoryQuery) OnlyX(ctx context.Context) *JobAssignmentHistory {
	node, err := jahq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only JobAssignmentHistory ID in the query.
// Returns a *NotSingularError when more than one JobAssignmentHistory ID is found.
// Returns a *NotFoundError when no entities are found.
func (jahq *JobAssignmentHistoryQuery) OnlyID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = jahq.Limit(2).IDs(setContextOp(ctx, jahq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{jobassignmenthistory.Label}
	default:
		err = &NotSingularError{jobassignmenthistory.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (jahq *JobAssignmentHistoryQuery) OnlyIDX(ctx context.Context) string {
	id, err := jahq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of JobAssignmentHistories.
func (jahq *JobAssignmentHistoryQuery) All(ctx context.Context) ([]*JobAssignmentHistory, error) {
	ctx = setContextOp(ctx, jahq.ctx, "All")
	if err := jahq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*JobAssignmentHistory, *JobAssignmentHistoryQuery]()
	return withInterceptors[[]*JobAssignmentHistory](ctx, jahq, qr, jahq.inters)
}

// AllX is like All, but panics if an error occurs.
func (jahq *JobAssignmentHistoryQuery) AllX(ctx context.Context) []*JobAssignmentHistory {
	nodes, err := jahq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of JobAssignmentHistory IDs.
func (jahq *JobAssignmentHistoryQuery) IDs(ctx context.Context) (ids []string, err error) {
	if jahq.ctx.Unique == nil && jahq.path != nil {
		jahq.Unique(true)
	}
	ctx = setContextOp(ctx, jahq.ctx, "IDs")
	if err = jahq.Select(jobassignmenthistory.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (jahq *JobAssignmentHistoryQuery) IDsX(ctx context.Context) []string {
	ids, err := jahq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (jahq *JobAssignmentHistoryQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, jahq.ctx, "Count")
	if err := jahq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, jahq, querierCount[*JobAssignmentHistoryQuery](), jahq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (jahq *JobAssignmentHistoryQuery) CountX(ctx context.Context) int {
	count, err := jahq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (jahq *JobAssignmentHistoryQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, jahq.ctx, "Exist")
	switch _, err := jahq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (jahq *JobAssignmentHistoryQuery) ExistX(ctx context.Context) bool {
	exist, err := jahq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the JobAssignmentHistoryQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (jahq *JobAssignmentHistoryQuery) Clone() *JobAssignmentHistoryQuery {
	if jahq == nil {
		return nil
	}
	return &JobAssignmentHistoryQuery{
		config:      jahq.config,
		ctx:         jahq.ctx.Clone(),
		order:       append([]jobassignmenthistory.OrderOption{}, jahq.order...),
		inters:      append([]Interceptor{}, jahq.inters...),
		predicates:  append([]predicate.JobAssignmentHistory{}, jahq.predicates...),
		withJob:     jahq.withJob.Clone(),
		withPartner: jahq.withPartner.Clone(),
		// clone intermediate query.
		sql:  jahq.sql.Clone(),
		path: jahq.path,
	}
}

// WithJob tells the query-builder to eager-load the nodes that are connected to
// the "job" edge. The optional arguments are used to configure the query builder of the edge.
func (jahq *JobAssignmentHistoryQuery) WithJob(opts ...func(*JobQuery)) *JobAssignmentHistoryQuery {
	query := (&JobClient{config: jahq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	jahq.withJob = query
	return jahq
}

// WithPartner tells the query-builder to eager-load the nodes that are connected to
// the "partner" edge. The optional arguments are used to configure the query builder of the edge.
func (jahq *JobAssignmentHistoryQuery) WithPartner(opts ...func(*PartnerQuery)) *JobAssignmentHistoryQuery {
	query := (&PartnerClient{config: jahq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	jahq.withPartner = query
	return jahq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"createdAt"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.JobAssignmentHistory.Query().
//		GroupBy(jobassignmenthistory.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (jahq *JobAssignmentHistoryQuery) GroupBy(field string, fields ...string) *JobAssignmentHistoryGroupBy {
	jahq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &JobAssignmentHistoryGroupBy{build: jahq}
	grbuild.flds = &jahq.ctx.Fields
	grbuild.label = jobassignmenthistory.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"createdAt"`
//	}
//
//	client.JobAssignmentHistory.Query().
//		Select(jobassignmenthistory.FieldCreatedAt).
//		Scan(ctx, &v)
func (jahq *JobAssignmentHistoryQuery) Select(fields ...string) *JobAssignmentHistorySelect {
	jahq.ctx.Fields = append(jahq.ctx.Fields, fields...)
	sbuild := &JobAssignmentHistorySelect{JobAssignmentHistoryQuery: jahq}
	sbuild.label = jobassignmenthistory.Label
	sbuild.flds, sbuild.scan = &jahq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a JobAssignmentHistorySelect configured with the given aggregations.
func (jahq *JobAssignmentHistoryQuery) Aggregate(fns ...AggregateFunc) *JobAssignmentHistorySelect {
	return jahq.Select().Aggregate(fns...)
}

func (jahq *JobAssignmentHistoryQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range jahq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, jahq); err != nil {
				return err
			}
		}
	}
	for _, f := range jahq.ctx.Fields {
		if !jobassignmenthistory.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if jahq.path != nil {
		prev, err := jahq.path(ctx)
		if err != nil {
			return err
		}
		jahq.sql = prev
	}
	return nil
}

func (jahq *JobAssignmentHistoryQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*JobAssignmentHistory, error) {
	var (
		nodes       = []*JobAssignmentHistory{}
		withFKs     = jahq.withFKs
		_spec       = jahq.querySpec()
		loadedTypes = [2]bool{
			jahq.withJob != nil,
			jahq.withPartner != nil,
		}
	)
	if jahq.withJob != nil || jahq.withPartner != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, jobassignmenthistory.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*JobAssignmentHistory).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &JobAssignmentHistory{config: jahq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(jahq.modifiers) > 0 {
		_spec.Modifiers = jahq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, jahq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := jahq.withJob; query != nil {
		if err := jahq.loadJob(ctx, query, nodes, nil,
			func(n *JobAssignmentHistory, e *Job) { n.Edges.Job = e }); err != nil {
			return nil, err
		}
	}
	if query := jahq.withPartner; query != nil {
		if err := jahq.loadPartner(ctx, query, nodes, nil,
			func(n *JobAssignmentHistory, e *Partner) { n.Edges.Partner = e }); err != nil {
			return nil, err
		}
	}
	for i := range jahq.loadTotal {
		if err := jahq.loadTotal[i](ctx, nodes); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (jahq *JobAssignmentHistoryQuery) loadJob(ctx context.Context, query *JobQuery, nodes []*JobAssignmentHistory, init func(*JobAssignmentHistory), assign func(*JobAssignmentHistory, *Job)) error {
	ids := make([]string, 0, len(nodes))
	nodeids := make(map[string][]*JobAssignmentHistory)
	for i := range nodes {
		if nodes[i].job_id == nil {
			continue
		}
		fk := *nodes[i].job_id
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(job.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "job_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (jahq *JobAssignmentHistoryQuery) loadPartner(ctx context.Context, query *PartnerQuery, nodes []*JobAssignmentHistory, init func(*JobAssignmentHistory), assign func(*JobAssignmentHistory, *Partner)) error {
	ids := make([]string, 0, len(nodes))
	nodeids := make(map[string][]*JobAssignmentHistory)
	for i := range nodes {
		if nodes[i].partner_id == nil {
			continue
		}
		fk := *nodes[i].partner_id
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(partner.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "partner_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (jahq *JobAssignmentHistoryQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := jahq.querySpec()
	if len(jahq.modifiers) > 0 {
		_spec.Modifiers = jahq.modifiers
	}
	_spec.Node.Columns = jahq.ctx.Fields
	if len(jahq.ctx.Fields) > 0 {
		_spec.Unique = jahq.ctx.Unique != nil && *jahq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, jahq.driver, _spec)
}

func (jahq *JobAssignmentHistoryQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(jobassignmenthistory.Table, jobassignmenthistory.Columns, sqlgraph.NewFieldSpec(jobassignmenthistory.FieldID, field.TypeString))
	_spec.From = jahq.sql
	if unique := jahq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if jahq.path != nil {
		_spec.Unique = true
	}
	if fields := jahq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, jobassignmenthistory.FieldID)
		for i := range fields {
			if fields[i] != jobassignmenthistory.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := jahq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := jahq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := jahq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := jahq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (jahq *JobAssignmentHistoryQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(jahq.driver.Dialect())
	t1 := builder.Table(jobassignmenthistory.Table)
	columns := jahq.ctx.Fields
	if len(columns) == 0 {
		columns = jobassignmenthistory.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if jahq.sql != nil {
		selector = jahq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if jahq.ctx.Unique != nil && *jahq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range jahq.modifiers {
		m(selector)
	}
	for _, p := range jahq.predicates {
		p(selector)
	}
	for _, p := range jahq.order {
		p(selector)
	}
	if offset := jahq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := jahq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// Modify adds a query modifier for attaching custom logic to queries.
func (jahq *JobAssignmentHistoryQuery) Modify(modifiers ...func(s *sql.Selector)) *JobAssignmentHistorySelect {
	jahq.modifiers = append(jahq.modifiers, modifiers...)
	return jahq.Select()
}

// JobAssignmentHistoryGroupBy is the group-by builder for JobAssignmentHistory entities.
type JobAssignmentHistoryGroupBy struct {
	selector
	build *JobAssignmentHistoryQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (jahgb *JobAssignmentHistoryGroupBy) Aggregate(fns ...AggregateFunc) *JobAssignmentHistoryGroupBy {
	jahgb.fns = append(jahgb.fns, fns...)
	return jahgb
}

// Scan applies the selector query and scans the result into the given value.
func (jahgb *JobAssignmentHistoryGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, jahgb.build.ctx, "GroupBy")
	if err := jahgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*JobAssignmentHistoryQuery, *JobAssignmentHistoryGroupBy](ctx, jahgb.build, jahgb, jahgb.build.inters, v)
}

func (jahgb *JobAssignmentHistoryGroupBy) sqlScan(ctx context.Context, root *JobAssignmentHistoryQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(jahgb.fns))
	for _, fn := range jahgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*jahgb.flds)+len(jahgb.fns))
		for _, f := range *jahgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*jahgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := jahgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// JobAssignmentHistorySelect is the builder for selecting fields of JobAssignmentHistory entities.
type JobAssignmentHistorySelect struct {
	*JobAssignmentHistoryQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (jahs *JobAssignmentHistorySelect) Aggregate(fns ...AggregateFunc) *JobAssignmentHistorySelect {
	jahs.fns = append(jahs.fns, fns...)
	return jahs
}

// Scan applies the selector query and scans the result into the given value.
func (jahs *JobAssignmentHistorySelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, jahs.ctx, "Select")
	if err := jahs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*JobAssignmentHistoryQuery, *JobAssignmentHistorySelect](ctx, jahs.JobAssignmentHistoryQuery, jahs, jahs.inters, v)
}

func (jahs *JobAssignmentHistorySelect) sqlScan(ctx context.Context, root *JobAssignmentHistoryQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(jahs.fns))
	for _, fn := range jahs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*jahs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := jahs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (jahs *JobAssignmentHistorySelect) Modify(modifiers ...func(s *sql.Selector)) *JobAssignmentHistorySelect {
	jahs.modifiers = append(jahs.modifiers, modifiers...)
	return jahs
}
