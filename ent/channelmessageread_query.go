// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"roofix/ent/channelmessage"
	"roofix/ent/channelmessageread"
	"roofix/ent/predicate"
	"roofix/ent/user"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// ChannelMessageReadQuery is the builder for querying ChannelMessageRead entities.
type ChannelMessageReadQuery struct {
	config
	ctx                *QueryContext
	order              []channelmessageread.OrderOption
	inters             []Interceptor
	predicates         []predicate.ChannelMessageRead
	withChannelMessage *ChannelMessageQuery
	withUser           *UserQuery
	withFKs            bool
	loadTotal          []func(context.Context, []*ChannelMessageRead) error
	modifiers          []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ChannelMessageReadQuery builder.
func (cmrq *ChannelMessageReadQuery) Where(ps ...predicate.ChannelMessageRead) *ChannelMessageReadQuery {
	cmrq.predicates = append(cmrq.predicates, ps...)
	return cmrq
}

// Limit the number of records to be returned by this query.
func (cmrq *ChannelMessageReadQuery) Limit(limit int) *ChannelMessageReadQuery {
	cmrq.ctx.Limit = &limit
	return cmrq
}

// Offset to start from.
func (cmrq *ChannelMessageReadQuery) Offset(offset int) *ChannelMessageReadQuery {
	cmrq.ctx.Offset = &offset
	return cmrq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (cmrq *ChannelMessageReadQuery) Unique(unique bool) *ChannelMessageReadQuery {
	cmrq.ctx.Unique = &unique
	return cmrq
}

// Order specifies how the records should be ordered.
func (cmrq *ChannelMessageReadQuery) Order(o ...channelmessageread.OrderOption) *ChannelMessageReadQuery {
	cmrq.order = append(cmrq.order, o...)
	return cmrq
}

// QueryChannelMessage chains the current query on the "channel_message" edge.
func (cmrq *ChannelMessageReadQuery) QueryChannelMessage() *ChannelMessageQuery {
	query := (&ChannelMessageClient{config: cmrq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := cmrq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := cmrq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(channelmessageread.Table, channelmessageread.FieldID, selector),
			sqlgraph.To(channelmessage.Table, channelmessage.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, channelmessageread.ChannelMessageTable, channelmessageread.ChannelMessageColumn),
		)
		fromU = sqlgraph.SetNeighbors(cmrq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryUser chains the current query on the "user" edge.
func (cmrq *ChannelMessageReadQuery) QueryUser() *UserQuery {
	query := (&UserClient{config: cmrq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := cmrq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := cmrq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(channelmessageread.Table, channelmessageread.FieldID, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, channelmessageread.UserTable, channelmessageread.UserColumn),
		)
		fromU = sqlgraph.SetNeighbors(cmrq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first ChannelMessageRead entity from the query.
// Returns a *NotFoundError when no ChannelMessageRead was found.
func (cmrq *ChannelMessageReadQuery) First(ctx context.Context) (*ChannelMessageRead, error) {
	nodes, err := cmrq.Limit(1).All(setContextOp(ctx, cmrq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{channelmessageread.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (cmrq *ChannelMessageReadQuery) FirstX(ctx context.Context) *ChannelMessageRead {
	node, err := cmrq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ChannelMessageRead ID from the query.
// Returns a *NotFoundError when no ChannelMessageRead ID was found.
func (cmrq *ChannelMessageReadQuery) FirstID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = cmrq.Limit(1).IDs(setContextOp(ctx, cmrq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{channelmessageread.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (cmrq *ChannelMessageReadQuery) FirstIDX(ctx context.Context) string {
	id, err := cmrq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ChannelMessageRead entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ChannelMessageRead entity is found.
// Returns a *NotFoundError when no ChannelMessageRead entities are found.
func (cmrq *ChannelMessageReadQuery) Only(ctx context.Context) (*ChannelMessageRead, error) {
	nodes, err := cmrq.Limit(2).All(setContextOp(ctx, cmrq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{channelmessageread.Label}
	default:
		return nil, &NotSingularError{channelmessageread.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (cmrq *ChannelMessageReadQuery) OnlyX(ctx context.Context) *ChannelMessageRead {
	node, err := cmrq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ChannelMessageRead ID in the query.
// Returns a *NotSingularError when more than one ChannelMessageRead ID is found.
// Returns a *NotFoundError when no entities are found.
func (cmrq *ChannelMessageReadQuery) OnlyID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = cmrq.Limit(2).IDs(setContextOp(ctx, cmrq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{channelmessageread.Label}
	default:
		err = &NotSingularError{channelmessageread.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (cmrq *ChannelMessageReadQuery) OnlyIDX(ctx context.Context) string {
	id, err := cmrq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ChannelMessageReads.
func (cmrq *ChannelMessageReadQuery) All(ctx context.Context) ([]*ChannelMessageRead, error) {
	ctx = setContextOp(ctx, cmrq.ctx, "All")
	if err := cmrq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ChannelMessageRead, *ChannelMessageReadQuery]()
	return withInterceptors[[]*ChannelMessageRead](ctx, cmrq, qr, cmrq.inters)
}

// AllX is like All, but panics if an error occurs.
func (cmrq *ChannelMessageReadQuery) AllX(ctx context.Context) []*ChannelMessageRead {
	nodes, err := cmrq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ChannelMessageRead IDs.
func (cmrq *ChannelMessageReadQuery) IDs(ctx context.Context) (ids []string, err error) {
	if cmrq.ctx.Unique == nil && cmrq.path != nil {
		cmrq.Unique(true)
	}
	ctx = setContextOp(ctx, cmrq.ctx, "IDs")
	if err = cmrq.Select(channelmessageread.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (cmrq *ChannelMessageReadQuery) IDsX(ctx context.Context) []string {
	ids, err := cmrq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (cmrq *ChannelMessageReadQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, cmrq.ctx, "Count")
	if err := cmrq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, cmrq, querierCount[*ChannelMessageReadQuery](), cmrq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (cmrq *ChannelMessageReadQuery) CountX(ctx context.Context) int {
	count, err := cmrq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (cmrq *ChannelMessageReadQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, cmrq.ctx, "Exist")
	switch _, err := cmrq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (cmrq *ChannelMessageReadQuery) ExistX(ctx context.Context) bool {
	exist, err := cmrq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ChannelMessageReadQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (cmrq *ChannelMessageReadQuery) Clone() *ChannelMessageReadQuery {
	if cmrq == nil {
		return nil
	}
	return &ChannelMessageReadQuery{
		config:             cmrq.config,
		ctx:                cmrq.ctx.Clone(),
		order:              append([]channelmessageread.OrderOption{}, cmrq.order...),
		inters:             append([]Interceptor{}, cmrq.inters...),
		predicates:         append([]predicate.ChannelMessageRead{}, cmrq.predicates...),
		withChannelMessage: cmrq.withChannelMessage.Clone(),
		withUser:           cmrq.withUser.Clone(),
		// clone intermediate query.
		sql:  cmrq.sql.Clone(),
		path: cmrq.path,
	}
}

// WithChannelMessage tells the query-builder to eager-load the nodes that are connected to
// the "channel_message" edge. The optional arguments are used to configure the query builder of the edge.
func (cmrq *ChannelMessageReadQuery) WithChannelMessage(opts ...func(*ChannelMessageQuery)) *ChannelMessageReadQuery {
	query := (&ChannelMessageClient{config: cmrq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	cmrq.withChannelMessage = query
	return cmrq
}

// WithUser tells the query-builder to eager-load the nodes that are connected to
// the "user" edge. The optional arguments are used to configure the query builder of the edge.
func (cmrq *ChannelMessageReadQuery) WithUser(opts ...func(*UserQuery)) *ChannelMessageReadQuery {
	query := (&UserClient{config: cmrq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	cmrq.withUser = query
	return cmrq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"createdAt"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ChannelMessageRead.Query().
//		GroupBy(channelmessageread.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (cmrq *ChannelMessageReadQuery) GroupBy(field string, fields ...string) *ChannelMessageReadGroupBy {
	cmrq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ChannelMessageReadGroupBy{build: cmrq}
	grbuild.flds = &cmrq.ctx.Fields
	grbuild.label = channelmessageread.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"createdAt"`
//	}
//
//	client.ChannelMessageRead.Query().
//		Select(channelmessageread.FieldCreatedAt).
//		Scan(ctx, &v)
func (cmrq *ChannelMessageReadQuery) Select(fields ...string) *ChannelMessageReadSelect {
	cmrq.ctx.Fields = append(cmrq.ctx.Fields, fields...)
	sbuild := &ChannelMessageReadSelect{ChannelMessageReadQuery: cmrq}
	sbuild.label = channelmessageread.Label
	sbuild.flds, sbuild.scan = &cmrq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ChannelMessageReadSelect configured with the given aggregations.
func (cmrq *ChannelMessageReadQuery) Aggregate(fns ...AggregateFunc) *ChannelMessageReadSelect {
	return cmrq.Select().Aggregate(fns...)
}

func (cmrq *ChannelMessageReadQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range cmrq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, cmrq); err != nil {
				return err
			}
		}
	}
	for _, f := range cmrq.ctx.Fields {
		if !channelmessageread.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if cmrq.path != nil {
		prev, err := cmrq.path(ctx)
		if err != nil {
			return err
		}
		cmrq.sql = prev
	}
	return nil
}

func (cmrq *ChannelMessageReadQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ChannelMessageRead, error) {
	var (
		nodes       = []*ChannelMessageRead{}
		withFKs     = cmrq.withFKs
		_spec       = cmrq.querySpec()
		loadedTypes = [2]bool{
			cmrq.withChannelMessage != nil,
			cmrq.withUser != nil,
		}
	)
	if cmrq.withChannelMessage != nil || cmrq.withUser != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, channelmessageread.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ChannelMessageRead).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ChannelMessageRead{config: cmrq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(cmrq.modifiers) > 0 {
		_spec.Modifiers = cmrq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, cmrq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := cmrq.withChannelMessage; query != nil {
		if err := cmrq.loadChannelMessage(ctx, query, nodes, nil,
			func(n *ChannelMessageRead, e *ChannelMessage) { n.Edges.ChannelMessage = e }); err != nil {
			return nil, err
		}
	}
	if query := cmrq.withUser; query != nil {
		if err := cmrq.loadUser(ctx, query, nodes, nil,
			func(n *ChannelMessageRead, e *User) { n.Edges.User = e }); err != nil {
			return nil, err
		}
	}
	for i := range cmrq.loadTotal {
		if err := cmrq.loadTotal[i](ctx, nodes); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (cmrq *ChannelMessageReadQuery) loadChannelMessage(ctx context.Context, query *ChannelMessageQuery, nodes []*ChannelMessageRead, init func(*ChannelMessageRead), assign func(*ChannelMessageRead, *ChannelMessage)) error {
	ids := make([]string, 0, len(nodes))
	nodeids := make(map[string][]*ChannelMessageRead)
	for i := range nodes {
		if nodes[i].channel_message_id == nil {
			continue
		}
		fk := *nodes[i].channel_message_id
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(channelmessage.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "channel_message_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (cmrq *ChannelMessageReadQuery) loadUser(ctx context.Context, query *UserQuery, nodes []*ChannelMessageRead, init func(*ChannelMessageRead), assign func(*ChannelMessageRead, *User)) error {
	ids := make([]string, 0, len(nodes))
	nodeids := make(map[string][]*ChannelMessageRead)
	for i := range nodes {
		if nodes[i].user_id == nil {
			continue
		}
		fk := *nodes[i].user_id
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(user.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "user_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (cmrq *ChannelMessageReadQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := cmrq.querySpec()
	if len(cmrq.modifiers) > 0 {
		_spec.Modifiers = cmrq.modifiers
	}
	_spec.Node.Columns = cmrq.ctx.Fields
	if len(cmrq.ctx.Fields) > 0 {
		_spec.Unique = cmrq.ctx.Unique != nil && *cmrq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, cmrq.driver, _spec)
}

func (cmrq *ChannelMessageReadQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(channelmessageread.Table, channelmessageread.Columns, sqlgraph.NewFieldSpec(channelmessageread.FieldID, field.TypeString))
	_spec.From = cmrq.sql
	if unique := cmrq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if cmrq.path != nil {
		_spec.Unique = true
	}
	if fields := cmrq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, channelmessageread.FieldID)
		for i := range fields {
			if fields[i] != channelmessageread.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := cmrq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := cmrq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := cmrq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := cmrq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (cmrq *ChannelMessageReadQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(cmrq.driver.Dialect())
	t1 := builder.Table(channelmessageread.Table)
	columns := cmrq.ctx.Fields
	if len(columns) == 0 {
		columns = channelmessageread.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if cmrq.sql != nil {
		selector = cmrq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if cmrq.ctx.Unique != nil && *cmrq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range cmrq.modifiers {
		m(selector)
	}
	for _, p := range cmrq.predicates {
		p(selector)
	}
	for _, p := range cmrq.order {
		p(selector)
	}
	if offset := cmrq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := cmrq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// Modify adds a query modifier for attaching custom logic to queries.
func (cmrq *ChannelMessageReadQuery) Modify(modifiers ...func(s *sql.Selector)) *ChannelMessageReadSelect {
	cmrq.modifiers = append(cmrq.modifiers, modifiers...)
	return cmrq.Select()
}

// ChannelMessageReadGroupBy is the group-by builder for ChannelMessageRead entities.
type ChannelMessageReadGroupBy struct {
	selector
	build *ChannelMessageReadQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (cmrgb *ChannelMessageReadGroupBy) Aggregate(fns ...AggregateFunc) *ChannelMessageReadGroupBy {
	cmrgb.fns = append(cmrgb.fns, fns...)
	return cmrgb
}

// Scan applies the selector query and scans the result into the given value.
func (cmrgb *ChannelMessageReadGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, cmrgb.build.ctx, "GroupBy")
	if err := cmrgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ChannelMessageReadQuery, *ChannelMessageReadGroupBy](ctx, cmrgb.build, cmrgb, cmrgb.build.inters, v)
}

func (cmrgb *ChannelMessageReadGroupBy) sqlScan(ctx context.Context, root *ChannelMessageReadQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(cmrgb.fns))
	for _, fn := range cmrgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*cmrgb.flds)+len(cmrgb.fns))
		for _, f := range *cmrgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*cmrgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := cmrgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ChannelMessageReadSelect is the builder for selecting fields of ChannelMessageRead entities.
type ChannelMessageReadSelect struct {
	*ChannelMessageReadQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (cmrs *ChannelMessageReadSelect) Aggregate(fns ...AggregateFunc) *ChannelMessageReadSelect {
	cmrs.fns = append(cmrs.fns, fns...)
	return cmrs
}

// Scan applies the selector query and scans the result into the given value.
func (cmrs *ChannelMessageReadSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, cmrs.ctx, "Select")
	if err := cmrs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ChannelMessageReadQuery, *ChannelMessageReadSelect](ctx, cmrs.ChannelMessageReadQuery, cmrs, cmrs.inters, v)
}

func (cmrs *ChannelMessageReadSelect) sqlScan(ctx context.Context, root *ChannelMessageReadQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(cmrs.fns))
	for _, fn := range cmrs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*cmrs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := cmrs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (cmrs *ChannelMessageReadSelect) Modify(modifiers ...func(s *sql.Selector)) *ChannelMessageReadSelect {
	cmrs.modifiers = append(cmrs.modifiers, modifiers...)
	return cmrs
}
